export async function sendSurveyEmail() {
  console.warn("Envio de email ainda não configurado no Supabase.");
  return { success: false };
}

export async function sendSurveyReminderEmail() {
  console.warn("Envio de lembrete ainda não configurado no Supabase.");
  return { success: false };
}